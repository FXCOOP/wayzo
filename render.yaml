services:
  - type: web
    name: wayzo-trip-planner
    env: node
    buildCommand: cd backend && npm install
    startCommand: node backend/server.mjs
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      # Force deployment trigger - staging 2025-10-06